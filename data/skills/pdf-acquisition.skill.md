# PDF Acquisition Skill

Download only public/legal PDFs when explicitly allowed. Validate PDF bytes, compute sha256 and byte count, record source URL, license hint, title-match score, timestamp, status, and failure reason in `docs/reference/pdf_manifest.json`.
