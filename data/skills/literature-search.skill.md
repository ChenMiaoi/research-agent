# Literature Search Skill

Use deterministic adapters to collect candidates without inventing papers. Run network search only when explicitly allowed, dedupe by identifiers/title, rank by relevance and CCF/venue signals, then write `docs/relative_work/candidates.json` and `search_report.md`.
