# PDF Reading Skill

Parse downloaded PDFs into stable page chunks, write `docs/reference/pdf_chunks.json`, and create `docs/reference/paper_notes/<paper_id>.md`. Every extracted claim must include page, quote, chunk id, confidence, and limitation context.
